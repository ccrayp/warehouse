worker_processes 1;

events {
    worker_connections 1024;
}

http {
    # upstream payment_service {
    #     server payment-1:8080;
    #     server payment-2:8080;
    # }

    server {
        listen 80 default_server;
        server_name _;

        # root /usr/share/nginx/html;
        # index index.html;

        # location /users {
        #     proxy_pass http://user:8080;
        # }
    }
}
